'use client';
import { FaWhatsapp, FaRocket } from 'react-icons/fa';

export default function Hero() {

    return (
        <section 
            id="home" 
            className="min-h-screen flex items-center justify-center pt-24 pb-16 px-4 bg-gradient-to-br from-blue-50 via-purple-50 to-pink-50">
        
        </section>
    );
}